<section>
  <div class="wrap">
    <%= link_to books_path, class:"button_area_blue back_btn p_item" do%>
      <span class="material-icons">arrow_back_ios</span>
      戻る
    <% end %>
      <h1><%= @book.name %></h1>
      <p class="book_author"><%= @book.author %></p>
      <p class="book_abstract border_sandwich"><%= @book.abstract %></p>
      <% @book_pages.each do |page| %>
          <%= image_tag page ,class:"book_page copy-guard"%>
      <% end %>
  </div>
</section>
