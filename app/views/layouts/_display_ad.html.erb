<section class="displayprof bg_black" style="background-image: url(<%= advertisement_image_path(ad.id) %>);">
    <div class="sponsor_info_area">
      <h1 class="sponsor">Powered by <%= ad.sponsor %></h1>
      <% if ad.url.present? %>
        <%= image_tag qrcode(ad.url), class: "ad_qrcode" %>
      <% end %>
    </div>
</section>